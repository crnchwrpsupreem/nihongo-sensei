# Waiting for the mini-PC publisher

The committed manifest is a placeholder until `scripts/publish_update.sh` completes successfully on the mini PC. A tutor must not begin corpus-grounded practice while `manifest.json` contains `"ready": false`.
